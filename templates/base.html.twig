<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Inventory application">
    <meta name="author" content="Sysadm.es - Víctor Gz">
    <title>{% block title %}{% endblock %}</title>
    <link rel="stylesheet"
          href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.3/css/bootstrap.min.css"
          integrity="sha384-Zug+QiDoJOrZ5t4lssLdxGhVrurbmBWopoEl+M6BdEfwnCJZtKxi1KgxUyJq13dy"
          crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
          rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
          rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/base.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body>
<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
    <h5 class="my-0 mr-md-auto font-weight-normal">Company name</h5>
    <nav class="my-2 my-md-0 mr-md-3">
        {% if app.User != null %}
            <a class="p-2 text-dark" href="{{ path('issues_index')}}"><i class="material-icons">event_note</i>Issue manager</a>
            <a class="p-2 text-dark" href="{{ path('hardware_index')}}"><i class="material-icons">laptop</i>Manage Hardware</a>
                {% with %}
                {% set lang = app.user.lang %}
                <button class="btn btn-outline-primary" type="button" data-toggle="dropdown">{% trans into lang %}Hello{% endtrans %}!, {{ app.user.username }}<i class="material-icons">arrow_drop_down</i></button>
                {% endwith %}
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('profile')}}"><i class="material-icons">person</i>My profile</a></li>
                <ul class="user-sub-menu">
                    <li><a class="dropdown-item" href="{{ path('reset_password')}}"><i class="material-icons">vpn_key</i>Reset Password</a></li>
                </ul>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a class="dropdown-item" href="{{ path('settings')}}"><i class="material-icons">settings</i>Settings</a></li>
                    <li><a class="dropdown-item" href="{{ path('user_index')}}"><i class="material-icons">people</i>Users</a></li>
                {% else %}
                {% endif %}
                <li role="separator" class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="{{ path('logout')}}"><i class="material-icons">https</i>Log out</a></li>
            </ul>
        {% else %}
            <a class="btn btn-outline-primary" href="{{ path('login')}}">Login</a>
            <a class="btn btn-outline-primary" href="{{ path('user_registration')}}">Sign up</a>
        {% endif %}
    </nav>
</div>
<div class="container">
    {% for message in app.session.flashbag() .get('status') %}
        <div class="alert alert-success">{{message}}</div>
    {% endfor %}
    {% block body %}{% endblock %}
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js" integrity="sha384-smHYKdLADwkXOn1EmN1qk/HfnUcbVRZyYmZ4qpPea6sjB/pTJ0euyQp0Mk8ck+5T" crossorigin="anonymous"></script>
    {% block javascripts %}{% endblock %}
    <footer class="pt-4 my-md-5 pt-md-5 border-top">
        <div class="row">
            <div class="col-6 col-md footer-col">
                <h5>Features</h5>
                <ul class="list-unstyled text-small">
                    <li><a class="text-muted" href="#">Managing items</a></li>
                    <li><a class="text-muted" href="{{ path('documentation') }}">Documentation</a></li>
                </ul>
            </div>
            <div class="col-6 col-md footer-col">
                <h5>Resources</h5>
                <ul class="list-unstyled text-small">
                    <li><a class="text-muted" href="{{ path('contact') }}">Contact</a></li>
                    <li><a class="text-muted" href="{{ path('privacy_policy') }}">Privacy policy</a></li>
                </ul>
            </div>
            <div class="col-6 col-md footer-col">
                <h5>About</h5>
                <ul class="list-unstyled text-small">
                    <li><a class="text-muted" href="https://github.com/VGzsysadm/Inventory-app">GitHub project</a></li>
                    <li><a class="text-muted" href="https://sysadm.es">Blog</a></li>
                </ul>
            </div>
        </div>
        <div class="row">
            <div class="col-6 col-md copyright-block">
                <small class="d-block mb-3 text-muted">&copy; 2018 application by Víctor - sysadm.es</small>
            </div>
        </div>
    </footer>
</div>
</body>
</html>

