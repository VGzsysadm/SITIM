{{ form_start(form, { 'attr': {'class': 'form-signin'} }) }}
    {{ form_row(form.email, {'label':false}) }}
    {{ form_row(form.lang, {'label':false}) }}
<a type="button" class="btn btn-secondary submit-user" href="{{ path('profile') }}">Back</a>
<button class="btn  btn-primary submit-user">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}